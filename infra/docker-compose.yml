version: '3.9'
services:
  postgres:
    image: postgres:16
    container_name: elpim-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: elpim
    ports:
      - '5432:5432'
    volumes:
      - elpim-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d elpim']
      interval: 5s
      timeout: 5s
      retries: 20

  redis:
    image: redis:7
    container_name: elpim-redis
    ports:
      - '6379:6379'
    command: redis-server --save '' --appendonly no
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 5s
      retries: 20

volumes:
  elpim-postgres-data:
